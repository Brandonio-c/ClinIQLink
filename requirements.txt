# PDF processing library
PyPDF2==3.0.1

# Data manipulation and CSV handling
pandas

# Hugging Face libraries for model support
transformers
tokenizers
accelerate>=0.26.0

# NLP library with model dependencies
spacy[lookups]  # Includes additional dependencies for language models
numpy

# Torch version for CUDA 12.1 (latest version available on BioWulf)
torch==2.1.0
torchvision
torchaudio
